node_modules/
.env
uploads/
*.log
.DS_Store
dist/
coverage/
.env.local

Tempcookie*.txt
*.docx
new-credentials.json
GUIDEON_Credentials.docx
.admin-credentials.txt

# App build artifacts (do not commit)
app/*.zip
*.aab
*.apk

# Local reference images — never commit (huge)
صور/
صور


# Official business documents — never commit (private)
الاوراق الرسمية/
*.pdf
!public/guides/*.pdf

# Android signing keystore — NEVER commit
android/android.keystore
android/*.keystore
android/app/
android/.gradle/

.env.production
# Local ops/debug scratch scripts (kept on disk, never committed)
deploy/_*
deploy/*.tgz
src/data/
